<h1>ClimbCast</h1>
<p>Your one stop solution to the age-old question "Should I go climbing today or stay in and watch Netflix?"  ClimbCast seeks to better inform you of current climbing conditions at your favorite areas with user submitted comments.</p>

<h1>Choose an Area:</h1>

<% @areas.each do |area| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
          <%= link_to area.title, area, class: 'btn btn-primary btnspace' %>
      </h4>
    </div>
  </div>
<% end %>

<% if current_user && current_user.admin? %>
  <h3>Administrator Options:</h3>
  <%= link_to "Add New Area", new_area_path, class: 'btn btn-primary btnspace bottom' %>
<% end %>
