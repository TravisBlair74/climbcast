<h1>Edit Area</h1>

<div class="row">
  <div class="col-md-4">
    <h3>Guidelines:</h3>
    <ul>
      <li>Only Admins should add areas.</li>
      <li>Make sure the area doesn't already exist.</li>
      <li>Think twice before you add those super secret locals only areas.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @area do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter area title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter area body" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
